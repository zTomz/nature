name: nature
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_hooks: ^0.20.5
  google_fonts: ^6.2.1
  provider: ^6.1.2
  auto_size_text:
    git:
      https://github.com/simc/auto_size_text.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/waterfall/